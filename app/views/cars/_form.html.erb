<%= simple_form_for(@car) do |f| %>
  <%= f.association :manufacturer, collection: @manufacturers %>
  <%= f.input :color %>
  <%= f.input :year, collection: (Date.today.year.to_i.downto(1920)) %>
  <%= f.input :mileage %>
  <%= f.input :description %>
  <%= f.button :submit %>
<% end %>
